---
const { title } = Astro.props;
---

<div class="flex flex-col items-start w-full border border-black">
	<div class="px-2 py-1 text-xl font-neue-haas-grotesk border-b border-black w-full">
		{title}
	</div>
	<div class="px-2 py-1 flex flex-col gap-2 w-full">
		<slot />
	</div>
</div>